\documentclass{article}

\usepackage{arxiv}
% RETTET: Tilføjet ] og ændret til numeric, så du kan lave hævede tal
\usepackage[backend=biber, style=numeric]{biblatex} 
\addbibresource{kilder.bib} 

\usepackage[utf8]{inputenc} 
\usepackage[T1]{fontenc} % RETTET: De usynlige mellemrum er renset væk
\usepackage{hyperref}       % hyperlinks
\usepackage{url}            % simple URL typesetting
\usepackage{booktabs}       % professional-quality tables
\usepackage{amsfonts}       % blackboard math symbols
\usepackage{nicefrac}       % compact symbols for 1/2, etc.
\usepackage{microtype}      % microtypography
\usepackage{graphicx}
\usepackage{float}
\usepackage{doi}
\usepackage{amsmath}

% --- Declaration of AI use: checklist environment ---
% A box label per item; \chk[x] renders a ticked box, \chk[] an empty one.
\newenvironment{checklist}{%
  \begin{list}{}{%
    \setlength{\leftmargin}{2em}%
    \setlength{\itemsep}{2pt}%
    \setlength{\parsep}{0pt}%
    \setlength{\topsep}{2pt}%
  }%
}{%
  \end{list}%
}
\newcommand{\chk}[1][]{\item[\fbox{\makebox[1.4ex][c]{\strut #1}}]}

\title{Multimodal capabilities of Gemma-4-E2B}

\author{
    Rune Christensen\\
    Artificial Intelligence \& Data  \\
    DTU \\
    \texttt{s250910@dtu.dk} \\
    \And
    Anas Alkaisi\\
    Artificial Intelligence \& Data\\
    DTU \\
    \texttt{s255797@dtu.dk} \\
    \And
    Frederik Naervig Gerlach-Hansen \\
    Artificial Intelligence \& Data \\
    DTU \\
    \texttt{s256931@dtu.dk} \\
}

% Uncomment to remove the date
%\date{}

\renewcommand{\shorttitle}{Multimodal capabilities of Gemma-4-E2B}

%%% Add PDF metadata
\hypersetup{
pdftitle={Multimodel capabilities of open source models},
pdfsubject={},
pdfauthor={},
pdfkeywords={},
}

\begin{document}
\maketitle

\begin{abstract}


The growing accessibility of locally deployable language models raises practical
concerns about their use in unsupervised academic assessments. This study
investigates the multimodal performance of Gemma-4-E2B, a 2-billion-parameter
open-source model that can be run on a standard student laptop, on 432
multiple-choice questions drawn from 16 publicly available examination sets for
the DTU course \textit{Introduction to Machine Learning and Data Mining}
(02450/02452). Questions were categorized into four input modalities: text-only,
image-only, and paired questions evaluated under both a screenshot condition and
a detailed textual description generated by an external model. Gemma achieved
73.8\% accuracy on text-only questions and 64.2\% on paired questions presented
as text, comfortably exceeding the 50\% passing threshold in both cases. By
contrast, accuracy fell to 44.8\% for paired questions shown as screenshots and
to 37.6\% for unpaired image questions, dropping below the passing threshold in
both image conditions. A McNemar test on the 134 paired questions confirmed that
the performance gap between the two presentation formats is statistically
significant ($\chi^2 = 15.36$, $p = 8.9 \times 10^{-5}$), and holds even when
question difficulty is held constant. A contamination check found no evidence
that Gemma was drawing on memorised answers from pre-cutoff exam material. These
results suggest that the model's weakness on visual inputs stems from difficulty
extracting numerical information from images rather than from limited subject
knowledge. For exam designers, the findings indicate that presenting tables and
matrices as images rather than typed text may be sufficient to push a small,
locally run model below the passing threshold, though this protection is likely
to diminish as open-source vision capabilities continue to improve.




\end{abstract}

\section*{Author contributions}
{\small\noindent
\textbf{Rune Christensen} (s250910): [describe contribution]. \\
\textbf{Anas Alkaisi} (s255797): [describe contribution]. \\
\textbf{Frederik Naervig Gerlach-Hansen} (s256931): [describe contribution].\par}
\newline
\textbf{Github link:} https://github.com/frederikngh/02445-gemma-evaluation


\newpage

\section{Introduction}
The rapid advancement of generative artificial intelligence has raised important questions regarding its impact on education and assessment. Modern AI systems are increasingly capable of solving complex academic tasks, including multiple-choice examination questions, which has led to concerns about academic integrity and the potential misuse of AI during unsupervised and supervised assessments. This can be seen in the "Generative AI use and misuse call for assessment reform in higher education"\supercite{chirikov2026genai} article published in Science. According to this article 9\% of undergrad students admit to using AI to cheat. At the same time, locally deployable models have become widely accessible, making it possible for students to run powerful AI systems directly on their personal computers without relying on cloud-based services. Understanding the capabilities and limitations of such models is therefore becoming increasingly relevant for both educators and students.

Motivated by these developments, this report investigates the performance of the locally deployable AI model Gemma-4-E2B on multiple-choice examination sets from the DTU course Introduction to Machine Learning and Data Mining (02450, 02452). Specifically, the study tests whether the model answers fewer questions correctly when a figure or table is shown as an image than when the same information is given as text. This addresses the multimodal evaluation gap described in Section~3.3 (p.~14) of "Sociotechnical Safety Evaluation of Generative AI Systems"\supercite{weidinger2023sociotechnical}. This is done to show the strengths and weaknesses of a locally run open-source model like Gemma, and to find out which kinds of questions are more AI-resistant: those given as text, or those that rely on figures and tables.

To evaluate the model, Gemma was tested on 16 publicly available examination sets spanning the years 2017–2025. A central objective of the study was to compare Gemma's performance on image-based and text-based prompts. For questions containing visualizations, such as tables and matrices, the model was evaluated under two separate conditions. In the first condition, Gemma received a screenshot of the visualization together with the original question text. In the second condition, the visualization was replaced by a detailed textual description generated using Anthropics Fable 5 model, while the original question text remained unchanged. This experimental setup allowed for a direct comparison of the model's ability to interpret graphical information versus equivalent textual descriptions.

The findings of this study provide insight into the practical capabilities of a locally run model like Gemma in an academic setting and contribute to the broader discussion surrounding the role of generative AI in modern education.



\section{Methods}
\subsection{The Dataset}

The dataset employed in this study consists of publicly available examination sets from the DTU course \textit{Introduction to Machine Learning and Data Mining} (02450 and 02452) spanning the years 2017--2025. Both the spring and fall examinations were included, resulting in a total of 16 exam sets. Each examination contains 27 multiple-choice questions, yielding a total sample size of 432 questions. These are real exams used to assess students, and each set is published together with an official solution, which we used as the answer key. Every question has exactly one correct answer, and the exams cover the course curriculum, including data preprocessing, clustering, classification, regression, and model evaluation.

The 432 questions differ in how the information needed to answer them is presented. 141 can be answered from text alone, 157 rely on a figure or plot --- such as a scatter plot, dendrogram, or ROC curve --- that cannot be faithfully written out as text, and the remaining 134 contain a table or matrix of numbers that can be presented either as an image or written out as text. The image versions of all figures and tables were cropped directly from the original exam PDFs.


\subsection{The Experimental Setup}

The objective of the experiment was to evaluate Gemma's ability to answer multiple-choice examination questions presented through different information modalities. The four different input modalities are presented below.

\begin{itemize}
    \item \textbf{Unpaired questions}: the question is presented in a single modality.
    \begin{itemize}
        \item \textbf{Unpaired text}: all the information needed is text. Gemma received the question text and answer options.
        \item \textbf{Unpaired image}: questions that have visualizations such as a graph, figure, or diagram that cannot be written out as text. Gemma received it as a screenshot together with the question text and answer options.
    \end{itemize}
    \item \textbf{Paired questions}: the question contains information that can be given both as text and as a screenshot, e.g. a matrix, vector, or table.
    \begin{itemize}
        \item \textbf{Paired text}: the visual element was first converted into a textual description using Anthropic's Fable 5. Gemma received this description together with the original question text and answer options.
        \item \textbf{Paired image}: Gemma received the original question text, answer options, and a screenshot of the associated visual element.
    \end{itemize}
\end{itemize}

\small{See appendix for examples of each question category}\newline


Unpaired examination questions can be considered independent, as each question is evaluated separately and does not share observations with other questions. Consequently, they produce unpaired (independent) observations. In contrast, the paired question is evaluated under both prompt modalities, resulting in paired observations. This pairing allows for a direct comparison between modalities using McNemar’s test as further explained in section 2.4.2.

The outcome metric in all analyses is accuracy, the proportion of questions answered correctly. Because each question has exactly one correct answer, the outcome is binary (correct or incorrect), and the dataset is not imbalanced, accuracy provides an appropriate measure of performance. This matters because accuracy can be misleading for imbalanced data, as shown in slide 5 of the Day 4 lecture material\supercite{das2026day4}.


\subsubsection{Prompting Strategy}

To obtain consistent and comparable responses across all questions, the same prompt structure was used throughout the experiment. Each question was submitted to Gemma with an instruction to reason through the problem step by step before providing a final answer. Without this explicit instruction, Gemma wouldn't use chain of thought and it would produce notably worse results. The model was required to end every response with the line \texttt{FINAL ANSWER: X}, where X is one of the answer options A--E. This format allowed the final answer to be extracted automatically from the model output.

To ensure reproducibility, Gemma was run using greedy decoding, meaning that the most likely next token was always selected rather than sampling from a probability distribution. Consequently, repeated evaluations of the same prompt would produce identical outputs.

The option E was included in every question as a ``don't know'' option, allowing Gemma to express uncertainty rather than being forced to guess. Since E was never the correct answer for any question, random guessing among the four valid options A--D yields a baseline accuracy of 25\%, which serves as the reference value in the binomial tests.

The prompt provided to Gemma was:

\begin{quote}
``You are answering a multiple-choice exam question from a university Machine Learning course. Each question has exactly one correct answer among A, B, C, D. You may also answer E if you genuinely do not know. Think the problem through step by step: write out your reasoning and any calculations first, but keep each step short and to the point. ALWAYS finish your reply with one line of exactly this form: FINAL ANSWER: X where X is a single capital letter A, B, C, D, or E.''
\end{quote}

Depending on the experimental condition described in Section 2.2, either textual content or visual information was appended to this prompt before being submitted to Gemma.

\subsection{The Model}
The model used in the project is Gemma-4-E2B. It is an open source language model developed by Google DeepMind. Gemma 4 is a multimodal model capable of processing both textual and visual inputs, making it suitable for the experimental design of this study, which involves both text and image processing. The model was selected due to its strong performance among open-source large language models. In official documentation, Google describes the Gemma family as “frontier-level performance at each size.”\supercite{gemma4modelcard}

Unlike closed models such as ChatGPT or Claude, Gemma 4 is released under an Apache 2.0 license, meaning it can be downloaded, run locally, and used freely. This was important for the project, since it allowed all experiments to be run on a local machine with full control over the setup, without any API calls and with no concerns about data leaving the local environment.

Running the model locally also made it straightforward to ensure statistical independence between observations, as each question could be sent in a completely fresh session with no shared state between calls. This follows the design used to ensure independence in Appendix~C.1 (p.~17) of "Evaluation of Large Language Models: STEM Education and Gender Stereotypes"\supercite{due2024evaluation}.\newline

The Gemma family consists of models of varying size, ranging from 2 to 31 billion parameters. The 2 billion parameter model (Gemma-4-E2B) was chosen so that the study could be reproduced by anyone with a standard computer. It was run on a desktop PC with 16 GB of VRAM. Of that 16 GB it used roughly 10 GB. A typical student laptop, for example a MacBook Air with 16 GB of unified memory, could therefore run it pretty comfortably.
\newline

Gemma 4 has a knowledge cutoff of January 2025\supercite{gemma4modelcard}, which introduces a potential problem. Any exam set published before this date may have been part of the model's training data, in which case Gemma would not be solving these questions itself, but it would rely on information it has already seen. This problem is known as data contamination, which is discussed as a limitation of benchmarks in Section~8.3 (p.~56) of "Evaluating Large Language Models: A Comprehensive Survey"\supercite{guo2023evaluating}, and would be a huge problem for this study, as it would inflate the accuracy scores of the exams before January 2025. This inflation of accuracy only affects exam sets from before the cutoff, but it doesn't affect exam sets after the cutoff.
To account for this, we subsequently perform a one-sided two-proportion z-test comparing the success rates on exam sets from before and after January 2025. This test provides evidence as to whether there is a statistically significant difference in performance between the two periods, which may indicate whether the pre-2025 exam material was included in Gemma’s training data.


\subsection{Statistical methods}

\subsubsection{Confidence intervals}

All accuracy estimates are reported with a 95\% confidence interval. Since each accuracy is a proportion of correct answers, we use the normal-approximation interval

$$\hat{p} \pm 1.96\sqrt{\frac{\hat{p}(1-\hat{p})}{n}},$$

where $\hat{p}$ is the observed accuracy and $n$ is the number of questions. The value $1.96$ is the $97.5\%$ quantile of the standard normal distribution. The test is valid if $np > 15$ and $n(1-p) > 15$; that is, the expected numbers of successes and failures must each be greater than $15$.

These intervals were computed in Python using the \texttt{proportion\_confint} function from \texttt{statsmodels}.

\subsubsection{McNemar's test}

McNemar's test was used to compare performance between the paired text and paired image modalities. Since the same questions were evaluated under both conditions, the resulting observations are paired, making McNemar's test an appropriate choice. Furthermore, the model output is binary (success/failure), corresponding to nominal data, which satisfies one of the assumptions of the test. McNemar's test is also a non-parametric procedure, meaning that it does not rely on distributional assumptions regarding the underlying population of the data. This study utilized the chi-squared approximation version of the test, as described in Section 4.3 of "Model Evaluation, Model Selection, and Algorithm Selection in Machine Learning"\supercite{raschka2018model}

The McNemar test is used to compare two paired methods when the outcome for each observation is binary (e.g., success or failure). Since both methods are applied to the same set of observations, the results are not independent and therefore cannot be analyzed using a standard two-sample proportion test. 

The paired outcomes are summarized in a $2\times2$ contingency table:

\begin{center}
\begin{tabular}{c c c c}
&
&
\multicolumn{2}{c}{\textbf{Method A}} \\
\cline{3-4}
&
&
\textbf{Success}
&
\textbf{Failure}
\\
\cline{2-4}
\multirow{}{}{\rotatebox[origin=c]{90}{\textbf{Method B}}}
&
\textbf{Success}
&
$a$
&
$b$
\\
&
\textbf{Failure}
&
$c$
&
$d$
\\
\cline{2-4}
\end{tabular}
\end{center}

The diagonal cells ($a$) and ($d$) contain the concordant pairs, where both methods produce the same outcome. These observations provide no evidence of a difference between the methods. Instead, McNemar's test focuses exclusively on the discordant pairs ($b$) and ($c$), which correspond to observations for which the two methods disagree. Under the null hypothesis,
$$
H_0 : p = 0.5,
$$

each method is equally likely to be correct among the discordant pairs. Consequently, conditional on the total number of discordant pairs ($n=b+c$), the number of observations in cell ($b$) follows a binomial distribution,

$$
b \sim \mathrm{Binomial}(n=b+c, p=0.5).
$$

For sufficiently large values when ($b+c \ge 25$), the McNemar chi-squared approximation test-statistic is used:

$$
\chi^2=\frac{(b-c)^2}{b+c}
$$

which approximately follows a chi-squared distribution with one degree of freedom under ($H_0$). For smaller samples, a continuity-corrected version known as Edwards' correction may be used:

$$
\chi^2=\frac{(|b-c|-1)^2}{b+c},
$$

or the exact McNemar test based on the binomial distribution may be preferred.

Since this report utilizes the chi-squared approximation method the corresponding $p$-value is found as:

$$p_{value} = P(\chi^2 > \chi^2_{obs})$$

A $p$-value less than our significance level $\alpha$ indicates that the observed imbalance between ($b$) and ($c$) is unlikely to have occurred by chance, providing evidence that one method tends to outperform the other on the paired observations.

This test was computed in Python using the \texttt{mcnemar} function from \texttt{statsmodels}, with \texttt{exact=False} and \texttt{correction=False} selecting the chi-squared approximation without continuity correction.

\subsubsection{Binomial tests}

This study considers six different estimated proportions. The first four correspond to the four individual input modalities, while the last two correspond to full-exam simulations that combine multiple input modalities . For each estimated proportion, two one-sided binomial hypothesis tests were conducted, resulting in 12 binomial tests overall. The first test assessed whether Gemma's accuracy exceeded the 25\% baseline expected under random guessing for a four-option multiple-choice exam, whereas the second assessed whether Gemma achieved a passing score by testing whether its accuracy exceeded the 50\% pass threshold.\newline
Of the two full-exam simulations, the first combined the unpaired-text, unpaired-image, and paired-text questions, while the second combined the unpaired-text, unpaired-image, and paired-image questions. This was done to provide insight into Gemma's performance on full exam sets for the two different prompting strategies for the paired questions.
\newline
All of the 12 binomial tests were one-sided tests since the purpose was to see how much better Gemma was than the guessing baseline and passing threshold.

The assumptions of a binomial test are 1) The outcomes should be binary, correct/incorrect, 2) Independent trials, the outcome of one trial shouldn't have an effect on the next one, meaning the probability for correct shouldn't change between trials. All of these assumptions are met in this study, and therefore a binomial test is a suitable test to analyze Gemma's performance.\newline 
\newline
The null hypothesis $H_0$ for a one-sided binomial test is $p_0=p$, and the alternative hypothesis $H_1$ is $p_0<p$\newline
The next step is to calculate the $p$-value. The CDF of a binomial distribution gives the probability of getting at most $x$ successes out of $n$ draws:

$$F(x) = P(X \le x) = \sum_{k=0}^{\lfloor x \rfloor} \binom{n}{k} p_0^{k} (1 - p_0)^{n-k}, \qquad \binom{n}{k} = \frac{n!}{k!(n - k)!}$$

Because the test is one-sided, we need the probability of getting at least as many correct answers as observed, which is the upper tail. The one-tailed p-value is therefore computed as:

$$p_{value} = P(X \ge x) = 1 - F(x-1)$$

Since each family contains six binomial tests asking the same question, there is a risk that one of them returns a significant result purely by chance. Running $m$ tests each at $\alpha = 0.05$ raises the family-wise error rate to approximately $1 - (1-0.05)^6=0.265$, so we apply the Bonferroni correction to control for this. The correction simply divides the significance threshold by the number of tests in the family:

\[
    \alpha_{\text{Bonf}} = \frac{\alpha}{m} = \frac{0.05}{6} = 0.00833
\]

This is the significance level we will compare our p-values to in the results section. Noteworthy, the Bonferroni method greatly reduces false positives (Type 1 errors) but increases the risk of missing true effects (Type 2 errors).

A possible problem might occur in that some of the binomial tests are dependent. If the accuracy for an input modality is high, the full exam simulation will also be high. This could be a problem since, according to the Day 2 slides\supercite{das2026day2}, Bonferroni assumes that the tests are independent. This independence is only needed if you want the family-wise error rate (FWER) to be roughly equal to $\alpha$, so $FWER \approx \alpha$. Since our binomial tests are not fully independent, FWER won't be roughly equal to $\alpha$. This means that we do not know our exact FWER. But since the FWER is the probability of getting one or more Type 1 errors, and dependence makes the Type 1 errors come in groups, so that either many happen at once or none happen, the FWER goes down as the positive dependence goes up. In other words, when the tests are positively dependent there are more scenarios where no Type 1 error happens than when they are independent. Applying Bonferroni therefore keeps $FWER \le \alpha$, so the true FWER becomes a bit lower than alpha, meaning that the probability of making a Type 1 error decreases. 

In this study a Type 1 error is worse than a Type 2 error. A Type 1 error would mean telling the course staff that figure and table questions are much more resistant to the model than they really are. This would give them a false sense of security and make them trust an exam format that does not actually stop students from using AI. A Type 2 error would only mean failing to spot a real difference between the modalities, therefore advising the staff to leave the exam sets as they are. Acting on a Type 1 error would therefore mean asking the Machine learning staff to change the entire exam format, when it shouldn't be changed. A Type 2 error would tell the Machine learning staff to leave the exam sets as they are. A Type 1 error is way more work and has bigger consequences for the Machine learning staff. It is clear why this study would rather make Type 2 errors than Type 1 errors.
\newline
The Day 2 slides also present the false discovery rate (FDR) procedure of Benjamini and Hochberg as an alternative to Bonferroni that has more power\supercite{das2026day2}. We still chose Bonferroni, since FDR gains its power by allowing a few Type 1 errors, and as explained above a Type 1 error is worse for us than a Type 2 error.
\newline
These tests were computed in Python using the \texttt{binomtest} function from \texttt{scipy}, with \texttt{alternative='greater'} for the one-sided tests.

\subsubsection{Two-proportion z-test}

The first z-test compares Gemma's accuracy on the unpaired-text questions with its accuracy on the unpaired-image questions. A significant difference would indicate that the success proportions differ between the two modalities, although such a difference could be due either to differences in question difficulty or to differences in Gemma's performance on textual and visual inputs. This is a two-sided test. The second z-test is a contamination check, which compares Gemma's accuracy on questions from before and after the model's knowledge cutoff, to see whether performance differs between the two time periods. This is done as a one-sided test.

The assumptions of a two-proportion z-test are 1) The outcomes should be binary, 2) The two groups should be independent, the questions in one group have no effect on the questions in the other, 3) The samples should be large enough for the normal approximation to hold, a common rule of thumb is the number of successes and failures must both be at least 10 respectively in each group. All of these assumptions are met in this study, and therefore a two-proportion z-test is a suitable test.\newline
\newline
The null hypothesis $H_0$ for the two-sided test is that the two groups have the same true accuracy, $p_1 = p_2$, and the alternative hypothesis $H_1$ is that the accuracies are different, $p_1 \neq p_2$. For the one-sided contamination check (before vs.\ after the knowledge cutoff) the hypotheses are instead directional, $H_0: p_{before} = p_{after}$ against $H_1: p_{before} > p_{after}$, testing specifically whether the pre-cutoff accuracy is inflated by training-data contamination.\newline
The test statistic is the difference between the two observed accuracies divided by the pooled standard error. Since the null hypothesis says the two accuracies are equal, the standard error is found from a pooled accuracy $\hat{p}$, which is found as:

$$\hat{p} = \frac{x_1 + x_2}{n_1 + n_2}$$

where $x_1$ and $x_2$ are the number of correct answers in each group and $n_1$ and $n_2$ are the respective sample sizes. The pooled test statistic is then:

$$z_{obs} = \frac{\hat{p}_1 - \hat{p}_2}{\sqrt{\hat{p}(1 - \hat{p})\left(\frac{1}{n_1} + \frac{1}{n_2}\right)}}$$

Under $H_0$ the $z$-statistic follows a standard normal distribution, so the p-value can be found from it and compared to the significance level $\alpha = 0.05$.

These tests were computed in Python using the \texttt{proportions\_ztest} function from \texttt{statsmodels}.

\subsubsection{Power}

Since the dataset is fixed and the number of questions can't be increased, the minimum effect-size detectable by the z-test at 80\% power and $\alpha = 0.05$ is now computed, following the sample-size formula for a difference in independent proportions given in slide~14 of the Day~4 lecture material\supercite{das2026day4}.

We start from the two-proportion sample-size formula, in which $p_1$ and $p_2$ are the true accuracies of the two groups,

$$n = \frac{(Z_{\alpha/2} + Z_{\beta})^2 \left[ p_1(1 - p_1) + p_2(1 - p_2) \right]}{(p_1 - p_2)^2}$$

and rearrange it to isolate the effect-size $p_1 - p_2$. Multiplying both sides by $(p_1 - p_2)^2$ and dividing by $n$,

$$(p_1 - p_2)^2 = \frac{(Z_{\alpha/2} + Z_{\beta})^2 \left[ p_1(1 - p_1) + p_2(1 - p_2) \right]}{n}$$

and taking the square root of both sides gives

$$p_1 - p_2 = (Z_{\alpha/2} + Z_{\beta})\sqrt{\frac{p_1(1 - p_1) + p_2(1 - p_2)}{n}}$$

To find the smallest effect-size detectable we set each term $p(1-p)$ at its maximum $0.25$, which occurs at $p = 0.5$. With $\alpha = 0.05$ (so $Z_{\alpha/2} = 1.96$), power $0.80$ (so $Z_{\beta} = 0.84$) and the smaller group size $n = 141$,

\begin{align*}
p_1 - p_2 &= (1.96 + 0.84)\sqrt{\frac{0.25 + 0.25}{141}} \\
&= 2.80 \cdot \sqrt{\frac{0.5}{141}} \\
&= 2.80 \cdot 0.0596 \\
&\approx 0.17
\end{align*}

So with this fixed $n$ the test can detect a true accuracy gap of $17$\% at least $80\%$ of the time. The gap that was observed in the Gemma run between the unpaired text and unpaired image questions is $0.738 - 0.376 = 0.36$, which is a lot higher than $0.17$. This means that the z-test is a good test to use in this study, and that it will almost always find a statistical significant difference between $p_{text}$ and $p_{graph}$ of 17\% or larger, if there is one.

The same method was used for the contamination check. That test is one-sided (so $Z_{\alpha} = 1.645$) and its smaller group is the $n = 54$ post-cutoff 2025 questions, so
$$p_1 - p_2 = (1.645 + 0.84)\sqrt{\frac{0.25 + 0.25}{54}} \approx 0.24.$$
With only 54 post-cutoff questions the check is therefore only well-powered to detect a large pre-cutoff inflation (about $24$\% or more). The differences actually observed (about $17$\% when the paired questions are scored as images and $12$\% as text) are smaller than this and run the opposite way, so the non-significant result should be read with this limited power in mind.

\section{Results}


\subsection{Summary of the data}




\begin{table}[H]
\centering
\caption{Accuracy of the Gemma~4 run, with each question counted once. Each exam contains 27 distinct questions; the paired (figure/table) questions are shown either as an image or as a text description, so the full-exam totals and the by-date rows are reported under both presentations. The 134 paired questions are listed once under \emph{Paired text} and once under \emph{Paired image}, so the total number of exam questions is $432 = 141 + 157 + 134$. Accuracy is $n_{correct}/n_{total}$; 95\% confidence intervals use the normal approximation.}
\label{tab:accuracy-ci}
\begin{tabular}{lrrrr}
\toprule
\textbf{Group} & $n_{correct}$ & $n_{total}$ & \textbf{Accuracy} & \textbf{95\% CI} \\
\midrule
\multicolumn{5}{@{}l}{\textit{Accuracy by input format}} \\
Unpaired text   &  104 & 141 & 73.8\% & [66.5,\,81.0] \\
Unpaired image  &  59 & 157 & 37.6\% & [30.0,\,45.2] \\
Paired text     &  86 & 134 & 64.2\% & [56.1,\,72.3] \\
Paired image    &  60 & 134 & 44.8\% & [36.4,\,53.2] \\
\midrule
\multicolumn{5}{@{}l}{\textit{Full-exam accuracy --- each question counted once}} \\
Unpaired text + unpaired image + paired image & 223 & 432 & 51.6\% & [46.9,\,56.3] \\
Unpaired text + unpaired image + paired text  & 249 & 432 & 57.6\% & [53.0,\,62.3] \\
\midrule
\multicolumn{5}{@{}l}{\textit{Accuracy by exam date --- paired shown as image}} \\
Exams 2017--2024    & 187 & 378 & 49.5\% & [44.4,\,54.5] \\
2025 exams combined &  36 &  54 & 66.7\% & [54.1,\,79.2] \\
\midrule
\multicolumn{5}{@{}l}{\textit{Accuracy by exam date --- paired shown as text}} \\
Exams 2017--2024    & 212 & 378 & 56.1\% & [51.1,\,61.1] \\
2025 exams combined &  37 &  54 & 68.5\% & [56.1,\,80.9] \\
\bottomrule
\end{tabular}
\end{table}









\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{modality.png}
    \caption{Accuracy for every group reported in Table~\ref{tab:accuracy-ci},
with 95\% confidence intervals: by input format, the full-exam totals (each
question counted once), and by exam date split at Gemma's January~2025
knowledge cutoff. Blue bars indicate text presentation, red bars indicate
image presentation; for the full-exam and by-date groups the colour reflects
whether the paired questions were shown as text or as an image. The dashed line
marks the 25\% chance baseline and the dotted line marks the 50\% passing
threshold.}
    \label{fig:placeholder}
\end{figure}













\subsection{McNemar's test}

\begin{center}
\begin{tabular}{c c c c}
&
&
\multicolumn{2}{c}{\textbf{Paired text}} \\
\cline{3-4}
&
&
\textbf{Success}
&
\textbf{Failure}
\\
\cline{2-4}
\multirow{}{}{\rotatebox[origin=c]{90}{\textbf{Paired image}}}
&
\textbf{Success}
&
51
&
9
\\
&
\textbf{Failure}
&
35
&
39
\\
\cline{2-4}
\end{tabular}
\end{center}

\vspace{5ex}
The contingency table summarizes the paired outcomes for the 134 paired questions evaluated using both prompting methods. Both methods correctly answered 51 questions, while both methods incorrectly answered 39 questions.

The paired image method correctly answered 9 questions that the paired text method answered incorrectly, whereas the paired text method correctly answered 35 questions that the paired image method answered incorrectly.

The total number of discordant pairs is therefore
\[
b + c = 9 + 35 = 44
\]

McNemar’s test is based solely on these discordant pairs. The corresponding empirical proportions are:
\[
\hat{p}_{\text{paired image}} = \frac{9}{44} \approx 0.205,
\qquad
\hat{p}_{\text{paired text}} = \frac{35}{44} \approx 0.795
\]

Under the null hypothesis, the two discordant outcomes are equally likely. In other words, among the questions where the methods disagree, each method is expected to be correct approximately 50\% of the time.

Since the number of discordant pairs is sufficiently large (\(b+c=44 \geq 25\)), the chi-square approximation without continuity correction can be used. The McNemar test-statistic is therefore

\[
\chi_{obs}^2
=
\frac{(b-c)^2}{b+c}
=
\frac{(9-35)^2}{9+35}
=
\frac{676}{44}
=
15.36
\]

With one degree of freedom, this corresponds to a \(p\)-value of approximately:

\[
p_{value} = P(\chi^2 > \chi^2_{obs}) =8.9 \times 10^{-5}
\]

Since the $p$-value is substantially smaller than the significance level $\alpha = 0.05$, there is strong evidence against the null hypothesis, leading to its rejection. This provides solid evidence that the two prompting methods do not perform equally. Therefore, the results suggest that the paired text prompting method significantly outperformed the paired image prompting method on this dataset.


\subsection{Binomial tests}

One-sided binomial tests were ran on each input format against two baselines: the 25\% accuracy expected from guessing ($H_0: p = 0.25$), and the 50\% DTU passing threshold ($H_0: p = 0.50$). Within each family of six, p-values were compared to the Bonferroni-corrected $\alpha_{\text{Bonf}}=0.00833$. The same test was also applied to two full-exam simulations that combine the different modalities as in a realistic exam.

\begin{center}
\begin{tabular}{lccccc}
\toprule
\textbf{Input format} & \textbf{Accuracy} & \textbf{$p_{value}$ (vs.\ 25\%)} & \textbf{\shortstack{Better than\\ guessing}} & \textbf{$p_{value}$ (vs.\ 50\%)} & \textbf{Pass} \\
\midrule
\multicolumn{6}{@{}l}{\textit{}} \\
Unpaired text         & 73.8\% & $8.8 \times 10^{-34}$ & Yes & $7.3 \times 10^{-9}$ & Yes \\
Unpaired image   & 37.6\% & $3.3 \times 10^{-4}$  & Yes & $> 0.99$             & No  \\
Paired text       & 64.2\% & $1.4 \times 10^{-21}$ & Yes & $6.5 \times 10^{-4}$ & Yes \\
Paired image & 44.8\% & $5.1 \times 10^{-7}$  & Yes & $0.90$               & No  \\
\midrule
\multicolumn{6}{@{}l}{\textit{Full-exam simulation}} \\
Unpaired text + unpaired image + paired image  & 51.6\% & $2.1 \times 10^{-32}$ & Yes & $0.27$               & No  \\
Unpaired text + unpaired image + paired text   & 57.6\% & $6.0 \times 10^{-47}$ & Yes & $8.7 \times 10^{-4}$ & Yes \\
\bottomrule
\end{tabular}
\end{center}

\vspace{2ex}

All modalities and both full exam simulations outperform random guessing, as all six $p$-values relative to the 25\% baseline are far below the corrected significance threshold. Against the 50\% passing threshold the formats split along modalities: Gemma passes for the two modalities unpaired text and paired text, but fails for unpaired image and paired image. The same pattern is observed in the full exam simulations. For the simulation in which the paired questions are presented as screenshots, Gemma only slightly exceeds the 50\% baseline. Since the corresponding $p$-value is $0.27$, the null hypothesis cannot be rejected, and there is insufficient evidence to conclude that Gemma performs above the 50\% threshold in this format. In contrast, when the paired questions are presented as text, the success rate increases to $57.6\%$. The corresponding $p$-value is below the significance level, indicating that the result is statistically significant, providing evidence that Gemma performs above the 50\% baseline in this setting.




\subsection{Two-proportion z-tests}

The two-proportion z-test was used for two comparisons: the unpaired-text vs.\ unpaired-image modality gap (two-sided), and the pre- vs.\ post-cutoff contamination check (one-sided).

\begin{center}
\begin{tabular}{lcccc}
\toprule
\textbf{Comparison} & \textbf{Accuracies} & \textbf{$z_{obs}$} & \textbf{$p_{value}$} & \textbf{Significant} \\
\midrule
Unpaired text vs. unpaired image (two-sided) & 73.8\% vs. 37.6\% & $6.26$  & $3.7 \times 10^{-10}$ & Yes \\
Pre/post-cutoff, paired as image (one-sided) & 49.5\% vs. 66.7\% & $-2.37$ & $0.99$ & No \\
Pre/post-cutoff, paired as text (one-sided)  & 56.1\% vs. 68.5\% & $-1.73$ & $0.96$ & No \\
\bottomrule
\end{tabular}
\end{center}

\vspace{2ex}

Gemma was far more accurate on unpaired text questions (73.8\%, $n = 141$) than on screenshots (37.6\%, $n = 157$), a highly significant 36 percentage point gap.

For the contamination check, a one-sided test compared pre-cutoff (2017--2024) accuracy against post-2025 accuracy, with each question counted once. The results run counter to what would be expected under data contamination, regardless of how the paired questions are prompted. Since performance is consistently higher on the 2025 exams in both conditions, there is no evidence of contamination in the dataset.

\section{Discussion}

\subsection{Interpretation of the results}

The most informative comparison in this study is the McNemar test, since it directly compared paired data for the two essential input modalities text and screenshots. The 134 
paired questions were evaluated under both modalities, meaning any difference in 
performance must be attributed to the format rather than the inherent difficulty 
of the questions. Among the 44 questions where the two modalities gave different 
answers, the paired text was correct 79.5\% of the time 
($\hat{p}_\text{text} = 35/44$) compared to only 20.5\% for the paired image
($\hat{p}_\text{image} = 9/44$). The McNemar test confirms this imbalance is 
statistically significant ($p_{value} = 8.9 \times 10^{-5}$) and extremely unlikely to be due to random variation.

This pattern is consistent with the binomial test results. Gemma exceeded the 50\% passing threshold for both unpaired and paired text questions, but fell below this threshold when the same information was presented as an image, in both cases. This difference suggests that the performance gap is not primarily due to reasoning ability, but rather to how the information is presented. When the input is given in text form, Gemma can reliably identify relevant values, interpret the structure of the problem, and select the correct answer. However, when the same information is embedded in images, errors are more likely to occur during the initial visual extraction step, where numerical values must be read from screenshots.

The two-proportion z-test comparing unpaired text (73.8\%) and unpaired image 
(37.6\%) questions also shows a large and significant gap, though this result is 
harder to interpret. Since these are entirely different questions, the difference 
in accuracy could reflect genuine modality effects, differences in question 
difficulty, or some combination of both. The McNemar test avoids this problem 
entirely by holding the question constant, which is why it is treated as the 
primary result.

The contamination check was included because most of the exam sets predate Gemma's January 2025 training cutoff, raising the possibility that some questions had been encountered during training. However, Gemma performed slightly better on the two 2025 exam sets than on the pre-2025 sets, which is opposite to what would be expected if memorization were artificially inflating performance. It is important to note that the post-2025 exam sets contained substantially fewer questions than the pre-2025 sets. This imbalance reduces the power of the hypothesis test, making it more difficult to detect a true difference between the two groups. Consequently, while the contamination analysis cannot conclusively rule out contamination at the level of individual questions, the results suggest that the observed performance reflects genuine model capabilities rather than simple recall.



\subsection{Limitations}
A significant limitation of this study are the comparisons of the unpaired text and image questions. Since these are entirely different questions, the difference 
in accuracy could reflect genuine modality effects, differences in question 
difficulty, or some combination of both. . The McNemar test 
sidesteps this, but it only covers tables and matrices, the paired set contains 
very few actual figures. Scatter plots, dendrograms, ROC curves: these only 
appear in the confounded unpaired comparison, so strictly speaking the main 
result applies to reading tables as images, not figures in general. The text descriptions used in the paired-text condition were generated by another 
AI model rather than written by hand. This matters because a well-written 
description might make the question genuinely easier to answer, not just easier 
to read. Some of Gemma's higher accuracy on paired-text questions could therefore 
reflect the quality of the description rather than a pure advantage of text over 
images. It is difficult to separate the two.

The study also only tests one model at one size, on one course. A larger version 
of Gemma, or a cloud-based model with a stronger vision component, might perform 
quite differently on the image questions. The weakness identified here is real, 
but it is specific to small, locally-run models, and it would be wrong to 
generalise the numbers to AI systems in general.

Two smaller points are worth noting. The contamination check rests on only two 
clean exam sets, which means it could only reliably detect a very large 
difference in performance around 24 percentage points or more. A subtler 
contamination effect would have gone undetected. And although each question was 
sent to Gemma in a fresh session, questions from the same exam are not fully 
independent: an unusually hard or easy exam will shift all of its questions in 
the same direction. This does not affect the McNemar test, but it could slightly 
distort the binomial tests and the z-test.

\subsection{Implications for exam design}
The introduction to this paper cited a study estimating that 9\% of undergraduate 
students use AI to cheat\supercite{chirikov2026genai}. The results 
here suggest that the risk is unevenly distributed across question types. A 
multiple-choice exam made entirely of text-based questions is one that a model 
small enough to run on a student laptop can pass. An exam that relies heavily on 
figures and tables presented as images is considerably harder for the same model 
to get through.

This does not require changing what an exam tests. The machine learning content 
can stay the same; what changes is whether the information is handed to the model 
as text or whether it has to be read off an image. For questions involving 
correlation matrices, confusion tables, or any other numerical data, showing the 
image rather than typing out the values appears to be enough to push a locally 
run open model below the passing threshold.

The caveat is that this protection is model-size dependent. A student with access 
to a larger cloud-based model might still get figure questions right. And as 
open-source vision models continue to improve, the gap will likely narrow. What 
works now may not work in two years. These findings are best understood as a 
snapshot of where small open models currently stand, not as a permanent 
structural feature of AI and visual information.

\section{Conclusion}

Gemma-4-E2B performs noticeably worse on unpaired image exam questions and on paired exam questions presented as screenshots compared to the corresponding text-based modalities. This is reflected in the lower accuracy on screenshot-based prompts, as evidenced by both the two-proportion z-test for unpaired text versus unpaired image questions and the McNemar test for the paired modalities. The weakness does not appear to stem from the model's understanding of machine learning itself, but rather from the modality through which the information is presented. In particular, Gemma exhibits lower performance when information must be extracted from images than when it is provided as plain text. While the observed limitation is specific to the Gemma-4-E2B model examined in this study, larger Gemma models, which require more powerful hardware, may possess stronger image-processing capabilities and therefore may not exhibit the same weaknesses. This distinction is important when evaluating the capabilities of AI models and their implications for academic integrity.


The contamination check found no evidence that Gemma was relying on memorized answers from earlier exams, suggesting that the observed results reflect genuine performance rather than training-data contamination.

For university exam designers and instructors, these findings have practical implications. Questions that require interpreting figures, graphs, or other visual information that cannot be faithfully represented as text appear to be considerably more resistant to this type of AI than purely text-based questions. Consequently, the modality through which information is presented should be taken into consideration when designing assessments.






\printbibliography[]

\vfill % Pushes the declaration to the bottom of the page if there is space

\section*{Declaration of AI use}

% Instructions
% --------------------------------------------------------------------------------
% Please check all items where you have used any form of generative AI
% to assist you. Be honest and specific—this helps us ensure academic
% integrity and proper acknowledgment.
% Check items by writing an "x" in the \chk[] command, i.e. \chk[x]

{ % BEGINNING OF AI DECLARATION
\small
\setlength{\parindent}{0pt}
I/we have used AI for the following tasks:

\medskip
\textbf{Writing / Text Generation}
\begin{checklist}
\chk[x] Rephrase or improve wording, grammar, or style.
\chk[x] Summarize external sources or content.
\chk[x] Check or enhance logical flow or structure of the report.
\chk[] Draft sections of the report.
\end{checklist}

\textbf{Research \& Information Gathering}
\begin{checklist}
\chk[] Find primary or secondary sources (papers, articles, data).
\chk[] Serve as a primary source of information or data.
\chk[x] Summarize or extract key points from sources.
\chk[x] Interpret or explain findings from literature.
\end{checklist}

\textbf{Idea Generation / Brainstorming}
\begin{checklist}
\chk[] Generate initial ideas or hypotheses for experiments or projects.
\chk[] Propose alternative approaches, solutions, or interpretations.
\chk[] Suggest research questions or objectives.
\chk[x] Identify potential limitations or challenges.
\end{checklist}

\textbf{Design of Experiments / Methodology}
\begin{checklist}
\chk[] Design experiments or workflows.
\chk[] Optimize parameters or settings for experiments.
\chk[] Propose control conditions or comparison groups.
\chk[x] Assist with experimental planning or sequencing of tasks.
\end{checklist}

\textbf{Computer Code / Programming}
\begin{checklist}
\chk[x] Debug code or identify errors.
\chk[x] Automate repetitive coding tasks.
\chk[] Explain programming concepts, algorithms, or logic.
\chk[] Optimize or refactor code for efficiency or readability.
\chk[x] Generate code snippets or full programs.
\end{checklist}

\textbf{Figures, Illustrations, \& Visualizations}
\begin{checklist}
\chk[] Propose visual design or layout of figures.
\chk[] Edit or format images for the report.
\chk[] Generate diagrams, schematics, or drawings.
\chk[x] Create or enhance graphs, plots, or charts.
\end{checklist}

\textbf{Data Analysis \& Interpretation}
\begin{checklist}
\chk[] Process or clean data.
\chk[] Perform statistical analyses.
\chk[] Interpret results or trends.
\chk[x] Generate tables or visual summaries of data.
\end{checklist}

\textbf{Other Uses of AI}
\begin{checklist}
\chk[x] Other (please specify):\\
Write LaTeX commands
\end{checklist}

\medskip
\textbf{Confirmation of No Inappropriate Use}
\begin{checklist}
\chk[x] I have not submitted AI-generated work as entirely my own without disclosure.
\chk[x] I have not used AI to fabricate or invent data, results, or references.
\chk[x] I have not used AI to plagiarize or copy others’ work without correct attribution.
\chk[x] I have not used AI to violate copyright, GDPR, or confidentiality requirements.
\chk[x] I have not used AI to bypass safety, ethical, or legal guidelines related to the project.
\end{checklist}

\paragraph{Declaration} I confirm that the above accurately reflects my use of generative AI in this report. I understand that failure to disclose AI use may be considered a breach of academic integrity.

% \onecolumn % Commented out to prevent accidental page breaks. Delete the '%' if you explicitly need a column reset here.
}

\section*{Appendix}

\begin{figure}[H]
    \centering
    \begin{minipage}[c]{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{images/Type A.png}
    \end{minipage}\hfill
    \begin{minipage}[c]{0.48\linewidth}
        \centering
        \includegraphics[width=\linewidth]{images/Type B.png}
    \end{minipage}
    \caption{Unpaired question examples: an unpaired text question (left) and an unpaired image question (right). The question on the left can't be presented as an image since it's purely text. The question on the right can't be presented as text because of the figures in the question.}
    \label{fig:unpaired-examples}
\end{figure}





\begin{figure}[H]
    \centering
    \fbox{\includegraphics[width=0.86\linewidth]{images/q25_exam_question_compact.png}}

    \vspace{1.5ex}

    \begin{minipage}[c]{0.42\linewidth}
        \centering
        \includegraphics[width=\linewidth]{images/image11.png}
    \end{minipage}\hfill
    \begin{minipage}[c]{0.54\linewidth}
        \centering
        \includegraphics[width=\linewidth]{images/image111.png}
    \end{minipage}
    \caption{Paired question example (Spring~2017, Q25), shown as it appears in
    the exam. The question stem and options (top) were identical in both
    conditions; only the confusion matrix (Figure~10) changed --- given to Gemma
    as a cropped image in the \textit{paired image} condition (bottom left), and
    as the text description written by Anthropic's Fable~5 that Gemma received in
    the \textit{paired text} condition (bottom right).}
    \label{fig:paired-examples}
\end{figure}









\end{document}


